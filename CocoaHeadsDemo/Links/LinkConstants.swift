public enum LinkConstants {
    public enum Configuration {
        enum Keys {
            static let webScheme = "WebScheme"
            static let webHost = "WebHost"
            static let firebaseDomain = "FirebaseDomain"
        }
        static let firebaseScheme = "dlscheme"
    }

    public enum QueryItemKeys {
        public enum ReferralLink {
            public static let referrerID = "referrer_id"
            public static let firstName = "referrer_first_name"
            public static let lastName = "referrer_last_name"
        }
    }
}
